# It works!!!!!

```av
int32 write(int32 fileno, void* mem, int32 bytes);
int32 main();
int32 main() {
  int8 c;
  c = 65_8;
  int8* ptr;
  ptr = &c;
  int32 succ;
  succ = write(1, ptr, 1);
  return 0;
}
```

SUCCESSFULLY COMPILES!