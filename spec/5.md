## Next plans

Great, now that the compiler works, let's make it somewhat nice to use.

## Addition 1

Instead of having to type:

```av
int32 x;
x = 5;
```

What if we could do 

```av
int32 x = 8;
```

Amazing, right?! This should be a straightforward addition, I'll only need to slightly modify the parser to detect Type Identifier Equal and change this to Type Identifier Semicolon Identifier Equal

## Addition 2

Let's also do the same thing for functions. So instead of 

```av
int32 main();
int32 main() {
...
```

We'd like to just be able to type:

```av
int32 main() {
...
```