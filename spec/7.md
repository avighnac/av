## If

It's time to add `if`.

Syntax:
```av
if (cond) {
  ...
}
```

Also, some bug fixes related to functions, label nesting, desugaring, etc.

## While

Same story here.

## Alloca

`alloca(bytes)` allocates ((bytes + 16) / 16) * 16 bytes of memory on the stack.